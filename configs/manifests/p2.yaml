# Fill before launching: image (Docker Hub) + repo_url (public GitHub).
# git_sha: null -> launcher pins current HEAD.
phase: p2
project: querent-attn
image: docker.io/rmamane/querent-deps:cu128-t271-v1
repo_url: https://github.com/rmamane/querent.git
git_sha: null
cells:
  - {experiment: p2/a0, seeds: [0, 1]}
  - {experiment: p2/a4_res, seeds: [0, 1]}
  - {experiment: p2/a4_replace, seeds: [0, 1]}
  - {experiment: p2/a4_res_posbias, seeds: [0, 1]}
  - {experiment: p2/a5, seeds: [0, 1]}
vast:
  gpu_name: RTX_3090   # dense cheap-bid supply, low contention; 5090 = fastest but contested (see drill A/B)
  num_gpus: 1
  price_max: 0.15
  disk: 64
  reliability_min: 0.99
  verified: true      # datacenter-vetted hosts only (wedged-pull/vanishing-host mitigations)
  inet_down_min: 200
  cuda_min: "12.8"   # cu128 container needs >=12.8 host driver (GeForce has no fwd-compat; error 804)
env_passthrough: [WANDB_API_KEY]
